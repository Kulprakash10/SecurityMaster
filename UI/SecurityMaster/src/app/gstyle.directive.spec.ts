import { GstyleDirective } from './gstyle.directive';

describe('GstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new GstyleDirective();
    expect(directive).toBeTruthy();
  });
});
